services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: teamchess
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-80}:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
